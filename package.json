{
  "name": "anime-black",
  "version": "1.0.0",
  "description": "Anime Black V7",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html manifest.json sw.js pwa-*.png apple-touch-icon.png icon.svg firebase-applet-config.json dist/ 2>/dev/null || true",
    "lint": "echo 'No linting required for vanilla JS'"
  },
  "dependencies": {
    "acorn": "^8.18.0",
    "puppeteer": "^25.10.0",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@babel/parser": "^8.0.5"
  }
}
