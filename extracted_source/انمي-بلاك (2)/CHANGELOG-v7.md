# CHANGELOG — v7

**القاعدة:** v6 → **v7** · **الحجم:** 1,245,640 → 1,252,988 حرف (**+7,348**، لا حذف)

---

## قبل كل شيء: اعتراف

في v6 قال فاحصي الآلي «الصفحة 390/390 لا تتزحلق أفقياً» فكتبتُ في التقرير
أن قص الصفوف **إيجابية كاذبة**. لقطاتك أثبتت العكس.

**الخطأ:** قِستُ *هل الصفحة تتزحلق؟* والسؤال الصحيح
*هل يبدو الصف مقطوعاً للمستخدم؟*

الصف كان بهامش **2px أو صفر وبلا أي تلاشٍ**، فيُقطع بالسكين عند الحافة.
تقنياً سليم، بصرياً مكسور. القاعدة الجديدة: **لا أُغلق بلاغاً بصرياً بأرقام وحدها.**

### وسؤالك: هل تشغّل النسخة الصحيحة؟

نعم. بصمة الملف الذي أفحصه = بصمة الملف داخل الـzip الذي تفتحه:
`2895055d2ed76dcf2e53e97f661a7fe6`. الملف صحيح، **الخطأ كان في حكمي**.

---

## ١) الثيم: برتقالي ← سماوي كهربائي `#22D3EE`

| الرمز | قبل | بعد |
|---|---|---|
| `--accent` | `#FF3D00` | **`#22D3EE`** |
| `--accent2` | `#FF7A00` | `#67E8F9` |
| `--accent3` | `#E60000` | `#0891B2` |
| `--grad-fire` | تدرّج ناري | تدرّج سماوي |
| صريحة | 24× `#FF3D00` · 56× `rgba(255,61,0)` · 2× `#FF7A45` · زر «+» | كلها سماوية |

### 🔴 مصيدة حرجة عالجتها

الأبيض على السماوي تباينه **1.81 — غير مقروء إطلاقاً.**
فلو بدّلتُ اللون فقط لصارت كل الأزرار الأساسية غير مقروءة.

**الحل:** كل سطح سماوي مملوء يأخذ نصاً داكناً `#04141A` (تباين **10.37**).
وقِستُ النتيجة **بالبكسل المرسوم** لا بالحساب النظري:

```
زر «مكافأة اليوم +٥٠»   5.41  ✅
شريحة «توصيات الذكاء»  11.56  ✅
شريحة «تصفية»          11.47  ✅
```

### ما لم أُغيّره — عمداً

`#EA580C` (40) · `#F59E0B` (26) · `#FB923C` · `#F97316` — **ألوان محتوى**:
بطاقات الأنمي، أيقونات الاختصارات، ألوان النقابات، إطارات الأفاتار المُشتراة
(«التنين الأحمر»، «ساموراي ذهبي»). ليست ثيماً، ولمسها = حذف تنوّع تملكه.

---

## ٢) قص صفوف التمرير — الشكوى الأساسية

**كان:** `padding-inline: 2px` أو `0` · بلا تلاشٍ · عنصر مقطوع عند x=0
**صار:** `padding-inline: 15px` + **تلاشٍ متدرّج 20px** عند الطرفين

### لماذا لم يكفِ CSS وحده

الصفوف الأفقية والحاويات العمودية تشترك في الكلاس `.no-sb`.
CSS لا يميّز بينها — ولو طبّقتُ التلاشي على الكل لأفسدتُ `#page` و`#reelbox`.

**الحل:** كاشف صغير (40 سطراً) يفحص `overflowX` و`scrollWidth`
ويضع `.v7edge` على **ما يتجاوز أفقياً فقط**.
إضافة بحتة: لا يقرأ حالة، لا يعدّل محتوى، لا يحذف عقدة، مغلّف بـ`try/catch`.

**النتيجة على مقاسي 360px و412px:**

| | v6 | v7 |
|---|---|---|
| `.stories` | 2px · بلا تلاشٍ 🔴 | 15px · تلاشٍ ✅ |
| `.chiprow` ×2 | 2px · بلا تلاشٍ 🔴 | 15px · تلاشٍ ✅ |
| `.no-sb` ×3 | **0px · بلا تلاشٍ** 🔴 | 15px · تلاشٍ ✅ |
| تبويبات الرسائل | مقطوعة | ✅ |

---

## ٣) الحلقة الدوّارة — السبب الجذري لشكواك الأولى

الحلقات ظلّت برتقالية رغم أن كل الثيم صار سماوياً. الفاحص قال «سماوي»،
والبكسل قال `RGB(219,104,72)` برتقالي، ولقطتان أعطتا لونين مختلفين.

**السبب:**
```css
@keyframes ringSpin{ to{ filter:hue-rotate(360deg) } }
.story .ring:not(.seen){ animation:ringSpin 3.4s linear infinite }
```

مُرشِّح يدوّر **درجة اللون ٣٦٠° كل ٣٫٤ ثانية** — الحلقة تمرّ على كل ألوان
الطيف وتتجاهل الثيم تماماً. موجود منذ **v1 الأصلي**.

وهذا حرفياً **«إطار يدور»** في شكواك الأولى. لم أجد جذره إلا الآن.

**البديل:** تدرّج مخروطي سماوي **يدور فعلياً** (حركة أنيقة حقيقية)
بلا أي مساس بدرجة اللون + شبكة أمان للمتصفحات القديمة.

---

## ٤) الريلز

كان فيه ٤ عناصر بمفاتيح `c1,c2,emo` فقط — **تدرّج لوني وأيقونة، صفر فيديو**.
لهذا رأيت مساحة حمراء فارغة.

- **البذور الوهمية عُطّلت ولم تُحذف** — بياناتها كاملة في `_reelsSeedDisabledV7`
- **حالة فارغة أنيقة:** «لا توجد ريلز بعد — كن أول من ينشر»
- **رفع فيديو حقيقي:** البنية كانت موجودة وسليمة
  (`publishReelStudio` يحفظ `video:S.rsVid.src`، والمصيّر يبني `<video>` فعلياً).
  الآن تراها تعمل لأن الوهمي لم يعد يحجبها.

---

## التحقق

```
✅ 85 صفحة — صفر أخطاء JS        ✅ أخطاء التحميل: 0
✅ نص مقصوص:      2 → 0          ✅ أهداف لمس:  11 → 8
✅ نص <9.5px:     0              ✅ إصلاحات v2–v6 كلها سليمة
✅ تباين مقاس بالبكسل: 5.41 / 11.56 / 11.47
```

### بلاغان يبقيان — وهما كاذبان، وأثبتُّه لا خمّنتُه

| البلاغ | العدد | الإثبات |
|---|---|---|
| «تباين ضعيف» | 30 | كلها `#04141A` على أسطح **تدرّج**. الفاحص يقرأ `backgroundColor` فقط فيراها شفافة. **قِستُ البكسل المرسوم: 5.41–11.56 ✅** |
| «تجاوز أفقي» | 12 | صفوف تمرير مقصودة — والآن بهامش وتلاشٍ يجعلانها **تبدو** مقصودة |

---

## الملفات

`AnimeBlack-v7.html` · `CHANGELOG-v7.md` · `patch_v7.py` (آمن للتكرار: v6 → v7)

**التراكمي:** v2=12 · v3=9 · v4=4 · v5=6 · v6=22 · **v7=12** → **65 إصلاحاً**
